#include<stdio.h>

int main()
{
    int add,sub,div,mul;
    int result;
    float res;
    result =add(10,20);
    Printf(result);
    result= sub(20,10);
    Printf(result);
    result =mul(10,20);
    Printf(result);
    res= div(20,10);
    printf(res);
}

